<%= form_with(model: Micotter.new) do |form| %>
    <div class="micotter_content">
      <%= form.label :content %>
      <%= form.text_field :content %>
    </div>
    <%= form.submit %>
  <% end %>